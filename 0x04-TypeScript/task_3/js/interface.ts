// Interface ts file
// Define and export the RowID type and RowElement interface

// Type alias for RowID
export type RowID = number;

// Interface for a row element
export interface RowElement {
    firstName: string;
    lastName: string;
    age?: number;
}
